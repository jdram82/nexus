# Project Notes

This folder contains the root-level project Markdown notes that were moved from the repository root to keep the workspace cleaner.

- Main project docs remain in `../` and `../docs/`
- Core root docs kept at root: `README.md`, `CHANGELOG.md`

If you had bookmarks to old root markdown files, update paths to:

`docs/project-notes/<filename>.md`
